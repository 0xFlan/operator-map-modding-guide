interface:
  display_name: "OPERATOR Unity Modding"
  short_description: "Build and verify native-quality OPERATOR maps"
  default_prompt: "Use $operator-unity-modding to build, repair, research, or verify a standalone OPERATOR Unity map and its exact-scene runtime integration."
